@using Bridge.WebApp.Pages.Admin.Components
@attribute [Route(PageRoutes.Admin.PlaceProductCreate)]
@attribute [Authorize(Policy = Bridge.Shared.Constants.PolicyConstants.Admin)]

<ProductForm FormMode="FormMode.Create" PlaceId="PlaceId" />

@code {
    [Parameter]
    public long PlaceId { get; set; }
}